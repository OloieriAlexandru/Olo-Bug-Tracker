import { BugsProjectsPipe } from './bugs-projects.pipe';

describe('BugsProjectsPipe', () => {
  it('create an instance', () => {
    const pipe = new BugsProjectsPipe();
    expect(pipe).toBeTruthy();
  });
});
